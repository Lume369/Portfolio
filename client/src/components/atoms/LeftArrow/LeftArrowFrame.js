import styled from "styled-components";

const LeftArrowFrame = styled.div`
    position: fixed;
    top: 6%;
    left: 3%;
    transform: translate(-50%,-50%);
    transform: rotate(180deg);
    width: 80px;
    height: 60px;
    cursor: pointer;
`;

export default LeftArrowFrame;
