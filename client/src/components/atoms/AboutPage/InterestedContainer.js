import styled from "styled-components";

const IntrestedContainer = styled.section`
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: left;
`;

export default IntrestedContainer;